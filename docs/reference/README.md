# Reference

Read [canon.source.yaml](../../canon.source.yaml) for proposed repository classes and [source-manifest.json](../../governance/adoption/source-manifest.json) for imported bytes. Unknown roots deny; only /canon is a candidate runtime source root. Each initial record is draft and runtime-ineligible. Schemas, compiler/governance, specifications, proposals, tests, generated output and engine source are excluded from content hydration.

The early three-layer explanation is Source Canon → normalized clauses/meaning ledger → resolved context. Contract types distinguish LocalCanonRelease, EffectiveCanonRelease/CanonBundle, ActorOverlayRelease, ContextCapsule, ContextSnapshot and CompilerRelease. ScopePack aliases require local/effective provenance; a snapshot records composition and evidence, not a new inference result.

Canon consumes KC2 semantics from talomnia-knowledge. Registry location and membership remain in workspace spaces files. Control and Talomnia are clients. Git main owns documents, Muneral owns tasks, Scrutator is derived, Auth owns identity and Vault/protected credential files own secrets. These are allocation rules, not deployed interfaces.
