# Applicability fixture requirements

Future executable fixtures must cover exact scope/authority/actor/action inputs, D0–D4 categorical boundaries, U4 refusal, unknown scope, package conflict, explicit prohibition ranked last, index outage, inherited locked policy, private actor overlays and revocation before effect. Use an independently enumerated source denominator. Seed definitions are not an executed test suite.
